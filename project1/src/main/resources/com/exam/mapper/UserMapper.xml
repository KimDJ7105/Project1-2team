<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.exam.mapper.UserMapper">

    <!-- 회원 가입 -->
    <insert id="signup" parameterType="UserDTO">
        insert into user( username, password, name, age)
        values ( #{username}, #{password}, #{name}, #{age} )
    </insert>

    <!-- 로그인 -->
    <select id="login" resultType="UserDTO" parameterType="String" >
        select userid, username, name, age from user
        where username = #{username}
    </select>

</mapper>
